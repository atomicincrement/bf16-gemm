[package]
name = "bf16-gemm"
version = "0.1.0"
edition = "2024"

[dependencies]
half = "2"

[dev-dependencies]
criterion = { version = "0.5", features = ["html_reports"] }

[build-dependencies]
cc = "1"
